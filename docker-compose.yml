services:
  dictation-app:
    build: .
    ports:
      - "8501:8501"
    # This is the section that enables Live Editing
    volumes:
      - ./app.py:/app/app.py
    extra_hosts:
      - "host.docker.internal:host-gateway"